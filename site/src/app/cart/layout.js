export const metadata = {
  title: "Your Cart — Vionics",
  description: "Review your selected Vionics products and proceed to secure checkout.",
};

export default function CartLayout({ children }) {
  return children;
}
