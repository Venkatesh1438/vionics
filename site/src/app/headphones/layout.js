export const metadata = {
  title: "AuraSound Pro — Vionics Headphones",
  description: "Experience audio in its purest form. 40mm custom drivers, AI spatial audio, 60-hour battery. Sound beyond dimensions.",
};

export default function HeadphonesLayout({ children }) {
  return children;
}
